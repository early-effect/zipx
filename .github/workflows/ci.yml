# Generated by zipx — do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: CI
"on":
  push:
    branches:
      - main
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
  pull_request: null
jobs:
  affected:
    name: affected
    runs-on: ubuntu-latest
    outputs:
      modules: ${{ steps.compute.outputs.modules }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@v1
      - name: Compute affected modules
        id: compute
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            base="${{ github.event.pull_request.base.sha }}"
            modules=$(sbt -batch --error "zipxAffectedModules $base")
          else
            modules='["all"]'
          fi
          echo "modules=$modules" >> "$GITHUB_OUTPUT"
  test-workflow:
    name: test workflow
    runs-on: ubuntu-latest
    needs:
      - affected
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'workflow') || contains(fromJson(needs.affected.outputs.modules), 'all'))"
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-0.1.0-SNAPSHOT
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: test
        run: sbt 'workflow/test'
  test-core:
    name: test core
    runs-on: ubuntu-latest
    needs:
      - affected
      - test-workflow
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'core') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.test-workflow.result != 'failure'"
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-0.1.0-SNAPSHOT
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: test
        run: sbt 'core/test'
  test-plugin:
    name: test plugin
    runs-on: ubuntu-latest
    needs:
      - affected
      - test-core
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'plugin') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.test-core.result != 'failure'"
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-0.1.0-SNAPSHOT
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: test
        run: sbt 'plugin/test'
  publish-workflow:
    name: publish workflow
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-0.1.0-SNAPSHOT
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: publish
        run: sbt 'workflow/publish'
  publish-core:
    name: publish core
    runs-on: ubuntu-latest
    needs:
      - publish-workflow
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-0.1.0-SNAPSHOT
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: publish
        run: sbt 'core/publish'
  publish-plugin:
    name: publish plugin
    runs-on: ubuntu-latest
    needs:
      - publish-core
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-0.1.0-SNAPSHOT
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: publish
        run: sbt 'plugin/publish'
