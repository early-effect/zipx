# Generated by zipx — do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: CI
"on":
  push:
    branches:
      - main
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
  pull_request: null
  workflow_dispatch: null
jobs:
  affected:
    name: affected
    runs-on: ubuntu-latest
    outputs:
      modules: ${{ steps.compute.outputs.modules }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Compute affected modules
        id: compute
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            BASE="${{ github.event.pull_request.base.sha }}"
            sbt -batch --error "zipxAffectedModules $BASE"
            modules=$(cat target/zipx-affected.json)
          else
            modules='["all"]'
          fi
          echo "modules=$modules" >> "$GITHUB_OUTPUT"
  test-workflow:
    name: test workflow
    runs-on: ubuntu-latest
    needs:
      - affected
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'workflow') || contains(fromJson(needs.affected.outputs.modules), 'all'))"
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: test
        run: sbt 'workflow/test'
  test-core:
    name: test core
    runs-on: ubuntu-latest
    needs:
      - affected
      - test-workflow
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'core') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.test-workflow.result != 'failure'"
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: test
        run: sbt 'core/test'
  test-central:
    name: test central
    runs-on: ubuntu-latest
    needs:
      - affected
      - test-core
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'central') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.test-core.result != 'failure'"
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: test
        run: sbt 'central/test'
  test-docs:
    name: test docs
    runs-on: ubuntu-latest
    needs:
      - affected
      - test-core
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'docs') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.test-core.result != 'failure'"
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: test
        run: sbt 'docs/test'
  test-plugin:
    name: test plugin
    runs-on: ubuntu-latest
    needs:
      - affected
      - test-central
      - test-core
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'plugin') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.test-central.result != 'failure' && needs.test-core.result != 'failure'"
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: test
        run: sbt 'plugin/test'
  publish-workflow:
    name: publish workflow
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    env:
      PGP_KEY_HEX: ${{ secrets.PGP_KEY_HEX }}
      PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
      SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
      SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: Import signing key
        run: |
          mkdir -p ~/.gnupg && chmod 700 ~/.gnupg
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          echo "pinentry-mode loopback"   >> ~/.gnupg/gpg.conf
          gpgconf --kill gpg-agent || true
          echo "$$PGP_SECRET" | base64 --decode | gpg --batch --import
        env:
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
      - name: publish
        run: sbt 'workflow/publishSigned'
  publish-core:
    name: publish core
    runs-on: ubuntu-latest
    needs:
      - publish-workflow
    if: startsWith(github.ref, 'refs/tags/v')
    env:
      PGP_KEY_HEX: ${{ secrets.PGP_KEY_HEX }}
      PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
      SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
      SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: Import signing key
        run: |
          mkdir -p ~/.gnupg && chmod 700 ~/.gnupg
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          echo "pinentry-mode loopback"   >> ~/.gnupg/gpg.conf
          gpgconf --kill gpg-agent || true
          echo "$$PGP_SECRET" | base64 --decode | gpg --batch --import
        env:
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
      - name: publish
        run: sbt 'core/publishSigned'
  publish-central:
    name: publish central
    runs-on: ubuntu-latest
    needs:
      - publish-core
    if: startsWith(github.ref, 'refs/tags/v')
    env:
      PGP_KEY_HEX: ${{ secrets.PGP_KEY_HEX }}
      PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
      SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
      SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: Import signing key
        run: |
          mkdir -p ~/.gnupg && chmod 700 ~/.gnupg
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          echo "pinentry-mode loopback"   >> ~/.gnupg/gpg.conf
          gpgconf --kill gpg-agent || true
          echo "$$PGP_SECRET" | base64 --decode | gpg --batch --import
        env:
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
      - name: publish
        run: sbt 'central/publishSigned'
  publish-plugin:
    name: publish plugin
    runs-on: ubuntu-latest
    needs:
      - publish-central
      - publish-core
    if: startsWith(github.ref, 'refs/tags/v')
    env:
      PGP_KEY_HEX: ${{ secrets.PGP_KEY_HEX }}
      PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
      SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
      SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: Import signing key
        run: |
          mkdir -p ~/.gnupg && chmod 700 ~/.gnupg
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          echo "pinentry-mode loopback"   >> ~/.gnupg/gpg.conf
          gpgconf --kill gpg-agent || true
          echo "$$PGP_SECRET" | base64 --decode | gpg --batch --import
        env:
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
      - name: publish
        run: sbt 'plugin/publishSigned'
  central-release:
    name: central-release
    runs-on: ubuntu-latest
    needs:
      - publish-central
      - publish-core
      - publish-plugin
      - publish-workflow
    if: startsWith(github.ref, 'refs/tags/v')
    env:
      PGP_KEY_HEX: ${{ secrets.PGP_KEY_HEX }}
      PGP_PASSPHRASE: ${{ secrets.PGP_PASSPHRASE }}
      SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
      SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@9d56cf12e9b58d219605e1d8bfe69a8395fedde0
      - name: Setup JDK 25
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95
        with:
          distribution: temurin
          java-version: "25"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk25-sbt-0.0.0-ci
          restore-keys: ubuntu-latest-jdk25-sbt-
      - name: Import signing key
        run: |
          mkdir -p ~/.gnupg && chmod 700 ~/.gnupg
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          echo "pinentry-mode loopback"   >> ~/.gnupg/gpg.conf
          gpgconf --kill gpg-agent || true
          echo "$$PGP_SECRET" | base64 --decode | gpg --batch --import
        env:
          PGP_SECRET: ${{ secrets.PGP_SECRET }}
      - name: central-release
        run: sbt 'sonaRelease'
  docs:
    name: docs
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: read
      pages: write
      id-token: write
    uses: early-effect/.github/.github/workflows/specular-docs.yml@main
    with:
      sbt-project: docs
