# Generated by zipx. Do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: zipx version updates
"on":
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch: null
permissions:
  contents: write
  pull-requests: write
jobs:
  version-updates:
    name: Catalog version updates
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          persist-credentials: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: version-updates
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "false"
      - name: Apply catalog updates
        run: |
          sbt "zipxDepUpdate yes"
          sbt "zipxActionUpdate yes"
          sbt "zipxPinUpdate yes"
          sbt zipxWorkflowGenerate

      - name: Open update PR
        run: |
          if [ -z "$(git status --porcelain)" ]; then
            echo "No catalog updates to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -B zipx/version-updates
          git add -A
          git restore --staged --worktree .github/workflows
          if [ -z "$(git diff --cached --name-only)" ]; then
            echo "No catalog updates to commit."
            exit 0
          fi
          git commit -m "ci: zipx version updates"
          git push -u origin HEAD
          gh pr create --title "ci: zipx version updates" --body "Applied zipxDepUpdate, zipxActionUpdate, and zipxPinUpdate." --head zipx/version-updates || true

