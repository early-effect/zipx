# Generated by zipx. Do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: CI
"on":
  push:
    branches:
      - main
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
  pull_request: null
  workflow_dispatch: null
concurrency:
  group: CI-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') && github.ref != 'refs/heads/main' }}
jobs:
  verify-gate:
    name: verify-gate
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
      pull-requests: read
    outputs:
      run: ${{ steps.check.outputs.run }}
    steps:
      - name: Skip Verify after merged PR push
        id: check
        run: |
          # Commits landed by merging/squashing a PR are associated with that PR via the API.
          # The commit-to-PR index can lag after squash; retry rather than fail-open into a second Verify.
          prs=0
          for attempt in 1 2 3 4 5 6; do
            if [ "$prs" -eq 0 ]; then
              if [ "$attempt" -eq 2 ]; then
                sleep 1
              elif [ "$attempt" -eq 3 ]; then
                sleep 2
              elif [ "$attempt" -eq 4 ]; then
                sleep 4
              elif [ "$attempt" -eq 5 ]; then
                sleep 8
              elif [ "$attempt" -eq 6 ]; then
                sleep 16
              fi
              prs=$(gh api "repos/${{ github.repository }}/commits/${{ github.sha }}/pulls" \
                --jq "[.[] | select(.merged_at != null and .base.ref == \"${{ github.ref_name }}\")] | length")
            fi
          done
          if [ "$prs" -gt 0 ]; then
            echo "Merged PR push, skipping redundant Verify (already ran on the PR)"
            echo "run=false" >> "$GITHUB_OUTPUT"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
  cache-rehydrate:
    name: cache-rehydrate
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: needs.verify-gate.result == 'success' && needs.verify-gate.outputs.run == 'false'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: cache-rehydrate
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: cache-rehydrate
        run: sbt 'compile'
  modver:
    name: modver
    runs-on: ubuntu-latest
    if: ((github.event_name == 'push') && ((github.ref == 'refs/heads/main'))) || (github.event_name == 'workflow_dispatch')
    permissions:
      contents: read
    outputs:
      modules: ${{ steps.compute.outputs.modules }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: modver
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "false"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: Compute version-moved modules
        id: compute
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            sbt -batch --error zipxModverPublishModules
            modules=$(cat target/zipx-modver-modules.json)
          elif [ "${{ github.event_name }}" = "push" ]; then
            before="${{ github.event.before }}"
            if [ -z "$before" ] || [ "$before" = "0000000000000000000000000000000000000000" ]; then
              echo "zipx: github.event.before is missing or all-zero; refusing to guess the publish set"
              exit 1
            else
              sbt -batch --error "zipxModverPublishModules $before"
              modules=$(cat target/zipx-modver-modules.json)
            fi
          else
            echo "zipx: github.event.before is missing or all-zero; refusing to guess the publish set"
            exit 1
          fi
          echo "modules=$modules" >> "$GITHUB_OUTPUT"
  test-L0:
    name: test L0
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: test-L0
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; models/testFull'
          else
            sbt 'models/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  test-L1:
    name: test L1
    runs-on: ubuntu-latest
    needs:
      - test-L0
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: test-L1
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; coreLib/testFull'
          else
            sbt 'coreLib/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  test-L2:
    name: test L2
    runs-on: ubuntu-latest
    needs:
      - test-L1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: test-L2
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; client/testFull; service/testFull'
          else
            sbt 'client/testFull; service/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: fmt
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: fmt
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; scalafmtCheckAll'
          else
            sbt 'scalafmtCheckAll'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  workflow-check:
    name: workflow-check
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: workflow-check
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: workflow-check
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxWorkflowCheck'
          else
            sbt 'zipxWorkflowCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  advisories:
    name: advisories
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: advisories
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: advisories
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxAdvisoryCheck'
          else
            sbt 'zipxAdvisoryCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  modver-suggest:
    name: modver-suggest
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: (!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))) && (github.event_name == 'pull_request')
    permissions:
      contents: read
      pull-requests: write
    env:
      ZIPX_MODVER_BASE_SHA: ${{ github.event.pull_request.base.sha }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: modver-suggest
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: Fetch PR base SHA
        run: git fetch --no-tags origin "$ZIPX_MODVER_BASE_SHA"
      - name: modver-suggest
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxModverSuggest'
          else
            sbt 'zipxModverSuggest'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  modver-check:
    name: modver-check
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: (!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))) && (github.event_name == 'pull_request')
    permissions:
      contents: read
    env:
      ZIPX_MODVER_BASE_SHA: ${{ github.event.pull_request.base.sha }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: modver-check
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: Fetch PR base SHA
        run: git fetch --no-tags origin "$ZIPX_MODVER_BASE_SHA"
      - name: modver-check
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxModverCheck'
          else
            sbt 'zipxModverCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  publish-models:
    name: publish models
    runs-on: ubuntu-latest
    needs:
      - modver
    if: "!cancelled() && (((github.event_name == 'push') && ((github.ref == 'refs/heads/main'))) || (github.event_name == 'workflow_dispatch')) && contains(fromJson(needs.modver.outputs.modules), 'models')"
    env:
      ZIPX_MODVER_REGISTRY: central
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: publish-models
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: publish
        run: sbt '+models/zipxModverPublishSigned'
  publish-coreLib:
    name: publish coreLib
    runs-on: ubuntu-latest
    needs:
      - modver
      - publish-models
    if: "!cancelled() && (((github.event_name == 'push') && ((github.ref == 'refs/heads/main'))) || (github.event_name == 'workflow_dispatch')) && contains(fromJson(needs.modver.outputs.modules), 'coreLib') && needs.publish-models.result != 'failure'"
    env:
      ZIPX_MODVER_REGISTRY: central
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: publish-coreLib
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: publish
        run: sbt '+coreLib/zipxModverPublishSigned'
  publish-client:
    name: publish client
    runs-on: ubuntu-latest
    needs:
      - modver
      - publish-coreLib
    if: "!cancelled() && (((github.event_name == 'push') && ((github.ref == 'refs/heads/main'))) || (github.event_name == 'workflow_dispatch')) && contains(fromJson(needs.modver.outputs.modules), 'client') && needs.publish-coreLib.result != 'failure'"
    env:
      ZIPX_MODVER_REGISTRY: central
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: publish-client
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: publish
        run: sbt '+client/zipxModverPublishSigned'
  docker:
    name: docker
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read
    env:
      ZIPX_EU_AWS_ACCOUNT_ID: "444455556666"
      ZIPX_EU_AWS_ECR_REGISTRY: "444455556666.dkr.ecr.eu-west-1.amazonaws.com"
      ZIPX_EU_AWS_REGION: eu-west-1
      ZIPX_EU_AWS_ROLE_TO_ASSUME: ${{ secrets.EU_REGISTRY_ROLE }}
      ZIPX_US_AWS_ACCOUNT_ID: "111122223333"
      ZIPX_US_AWS_ECR_REGISTRY: "111122223333.dkr.ecr.us-east-1.amazonaws.com"
      ZIPX_US_AWS_REGION: us-east-1
      ZIPX_US_AWS_ROLE_TO_ASSUME: ${{ secrets.US_REGISTRY_ROLE }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: docker
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: zipx AWS login (eu)
        uses: ./.github/actions/zipx-aws-login
        with:
          role-env: ZIPX_EU_AWS_ROLE_TO_ASSUME
          region-env: ZIPX_EU_AWS_REGION
          account-env: ZIPX_EU_AWS_ACCOUNT_ID
          login-ecr: "true"
          name-suffix: eu
      - name: zipx AWS login (us)
        uses: ./.github/actions/zipx-aws-login
        with:
          role-env: ZIPX_US_AWS_ROLE_TO_ASSUME
          region-env: ZIPX_US_AWS_REGION
          account-env: ZIPX_US_AWS_ACCOUNT_ID
          login-ecr: "true"
          name-suffix: us
      - name: docker
        run: sbt 'service/Docker/publish'
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs:
      - docker
    if: startsWith(github.ref, 'refs/tags/v')
    environment: ${{ matrix.environment }}
    permissions:
      id-token: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: prod
            environment: production
            AWS_REGION: us-east-1
            AWS_ROLE_TO_ASSUME: ${{ secrets.PROD_DEPLOY_ROLE }}
            TIER: prod
          - target: staging
            AWS_REGION: us-west-2
            AWS_ROLE_TO_ASSUME: ${{ secrets.STAGING_DEPLOY_ROLE }}
            TIER: staging
    env:
      AWS_REGION: ${{ matrix.AWS_REGION }}
      AWS_ROLE_TO_ASSUME: ${{ matrix.AWS_ROLE_TO_ASSUME }}
      TIER: ${{ matrix.TIER }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: deploy
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: "8f2e77a00c7c0f58"
      - name: zipx AWS login
        uses: ./.github/actions/zipx-aws-login
        with:
          role-env: AWS_ROLE_TO_ASSUME
          region-env: AWS_REGION
          account-env: AWS_ACCOUNT_ID
          login-ecr: "false"
          name-suffix: ""
      - name: deploy
        run: sbt 'service/promote'
