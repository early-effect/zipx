# Generated by zipx. Do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: CI
"on":
  push:
    branches:
      - main
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
  pull_request: null
concurrency:
  group: CI-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}
jobs:
  verify-gate:
    name: verify-gate
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
      pull-requests: read
    outputs:
      run: ${{ steps.check.outputs.run }}
    steps:
      - name: Skip Verify after merged PR push
        id: check
        run: |
          # Commits landed by merging/squashing a PR are associated with that PR via the API.
          prs=$(gh api "repos/${{ github.repository }}/commits/${{ github.sha }}/pulls" \
            --jq "[.[] | select(.merged_at != null and .base.ref == \"${{ github.ref_name }}\")] | length")
          if [ "$prs" -gt 0 ]; then
            echo "Merged PR push, skipping redundant Verify (already ran on the PR)"
            echo "run=false" >> "$GITHUB_OUTPUT"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
  cache-rehydrate:
    name: cache-rehydrate
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: needs.verify-gate.result == 'success' && needs.verify-gate.outputs.run == 'false'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-cache-rehydrate
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: cache-rehydrate
        run: sbt 'compile'
  test-L0:
    name: test L0
    runs-on: ubuntu-latest
    needs:
      - fmt
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-test-L0
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; models/testFull'
          else
            sbt 'models/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  test-L1:
    name: test L1
    runs-on: ubuntu-latest
    needs:
      - test-L0
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-test-L1
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; coreLib/testFull'
          else
            sbt 'coreLib/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  test-L2:
    name: test L2
    runs-on: ubuntu-latest
    needs:
      - test-L1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-test-L2
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; client/testFull; service/testFull'
          else
            sbt 'client/testFull; service/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-fmt
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: fmt
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; scalafmtCheckAll'
          else
            sbt 'scalafmtCheckAll'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  publish-L0:
    name: publish L0
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-publish-L0
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: publish
        run: sbt '+models/publish'
  publish-L1:
    name: publish L1
    runs-on: ubuntu-latest
    needs:
      - publish-L0
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-publish-L1
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: publish
        run: sbt '+coreLib/publish'
  publish-L2:
    name: publish L2
    runs-on: ubuntu-latest
    needs:
      - publish-L1
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-publish-L2
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: publish
        run: sbt '+client/publish'
  docker:
    name: docker
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read
    env:
      ZIPX_EU_AWS_ACCOUNT_ID: "444455556666"
      ZIPX_EU_AWS_ECR_REGISTRY: "444455556666.dkr.ecr.eu-west-1.amazonaws.com"
      ZIPX_EU_AWS_REGION: eu-west-1
      ZIPX_EU_AWS_ROLE_TO_ASSUME: ${{ secrets.EU_REGISTRY_ROLE }}
      ZIPX_US_AWS_ACCOUNT_ID: "111122223333"
      ZIPX_US_AWS_ECR_REGISTRY: "111122223333.dkr.ecr.us-east-1.amazonaws.com"
      ZIPX_US_AWS_REGION: us-east-1
      ZIPX_US_AWS_ROLE_TO_ASSUME: ${{ secrets.US_REGISTRY_ROLE }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-docker
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: Assume AWS role (OIDC, eu)
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ env.ZIPX_EU_AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.ZIPX_EU_AWS_REGION }}
      - name: Log in to ECR (eu)
        uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64
        with:
          registries: ${{ env.ZIPX_EU_AWS_ACCOUNT_ID }}
      - name: Assume AWS role (OIDC, us)
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ env.ZIPX_US_AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.ZIPX_US_AWS_REGION }}
      - name: Log in to ECR (us)
        uses: aws-actions/amazon-ecr-login@d539f0932e70871a027e9d5a9d8fc38589180a64
        with:
          registries: ${{ env.ZIPX_US_AWS_ACCOUNT_ID }}
      - name: docker
        run: sbt 'service/Docker/publish'
  deploy-prod:
    name: deploy (prod)
    runs-on: ubuntu-latest
    needs:
      - docker
    if: startsWith(github.ref, 'refs/tags/v')
    environment: production
    permissions:
      id-token: write
      contents: read
    env:
      AWS_REGION: us-east-1
      AWS_ROLE_TO_ASSUME: ${{ secrets.PROD_DEPLOY_ROLE }}
      TIER: prod
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-deploy-prod
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: Assume AWS role (OIDC)
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ env.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}
      - name: deploy
        run: sbt 'service/promote'
  deploy-staging:
    name: deploy (staging)
    runs-on: ubuntu-latest
    needs:
      - docker
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read
    env:
      AWS_REGION: us-west-2
      AWS_ROLE_TO_ASSUME: ${{ secrets.STAGING_DEPLOY_ROLE }}
      TIER: staging
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: Setup JDK 21
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@bfea3c5f48abd221b04a6df4798aa5eb8b6a2baf # v1.5.6
        with:
          disk-cache: "false"
      - name: Resolve cache epoch
        id: cache-epoch
        run: |
          set -euo pipefail
          tag_match='v*'
          if git remote get-url origin >/dev/null 2>&1; then
            remote_count=$(git ls-remote --tags --refs origin "$tag_match" 2>/dev/null | wc -l | tr -d ' ')
            local_count=$(git tag -l "$tag_match" | wc -l | tr -d ' ')
            if [ "$remote_count" -gt 0 ] && [ "$local_count" -lt "$remote_count" ]; then
              echo "::warning title=zipx cache epoch::Local tags ($local_count) fewer than origin ($remote_count). Epoch may be stale; checkout must use fetch-depth: 0 and fetch-tags: true."
            fi
          fi
          if [[ "${GITHUB_REF:-}" == refs/tags/v* ]]; then
            release="${GITHUB_REF#refs/tags/v}"
            epoch="$release"
          elif tag=$(git describe --tags --abbrev=0 --match "$tag_match" 2>/dev/null); then
            release="${tag#v}"
            epoch="${release}-ci"
          else
            echo "::warning title=zipx cache epoch::No tags matching '$tag_match' found; using epoch 0.0.0."
            release="0.0.0"
            epoch="0.0.0"
          fi
          echo "epoch=$epoch" >> "$GITHUB_OUTPUT"
          echo "release=$release" >> "$GITHUB_OUTPUT"

      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-deploy-staging
          restore-keys: |
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.epoch }}-
            ubuntu-latest-jdk21-sbt-${{ steps.cache-epoch.outputs.release }}-
            ubuntu-latest-jdk21-sbt-
      - name: Assume AWS role (OIDC)
        uses: aws-actions/configure-aws-credentials@e6de054238d6b7531b4efff3b6587d9aade6a06c # v6.2.3
        with:
          role-to-assume: ${{ env.AWS_ROLE_TO_ASSUME }}
          aws-region: ${{ env.AWS_REGION }}
      - name: deploy
        run: sbt 'service/promote'
