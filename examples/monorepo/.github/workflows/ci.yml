# Generated by zipx — do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: CI
"on":
  push:
    branches:
      - main
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
  pull_request: null
jobs:
  verify-gate:
    name: verify-gate
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
      pull-requests: read
    outputs:
      run: ${{ steps.check.outputs.run }}
    steps:
      - name: Skip Verify after merged PR push
        id: check
        run: |
          # Commits landed by merging/squashing a PR are associated with that PR via the API.
          prs=$(gh api "repos/${{ github.repository }}/commits/${{ github.sha }}/pulls" \
            --jq "[.[] | select(.merged_at != null and .base.ref == \"${{ github.ref_name }}\")] | length")
          if [ "$prs" -gt 0 ]; then
            echo "Merged PR push — skipping redundant Verify (already ran on the PR)"
            echo "run=false" >> "$GITHUB_OUTPUT"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
  test-L0:
    name: test L0
    runs-on: ubuntu-latest
    needs:
      - fmt
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-test-L0
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: zipx test
        run: sbt 'models/test'
  test-L1:
    name: test L1
    runs-on: ubuntu-latest
    needs:
      - fmt
      - test-L0
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-test-L1
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: zipx test
        run: sbt 'coreLib/test'
  test-L2:
    name: test L2
    runs-on: ubuntu-latest
    needs:
      - fmt
      - test-L1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-test-L2
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: zipx test
        run: sbt 'client/test; service/test'
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-fmt
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: zipx fmt
        run: sbt 'scalafmtCheckAll'
  publish-L0:
    name: publish L0
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-publish-L0
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: zipx publish
        run: sbt '+models/publish'
  publish-L1:
    name: publish L1
    runs-on: ubuntu-latest
    needs:
      - publish-L0
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-publish-L1
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: zipx publish
        run: sbt '+coreLib/publish'
  publish-L2:
    name: publish L2
    runs-on: ubuntu-latest
    needs:
      - publish-L1
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-publish-L2
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: zipx publish
        run: sbt '+client/publish'
  docker-service-eu:
    name: docker service (eu)
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read
    env:
      DEPLOY_ROLE: ${{ secrets.EU_REGISTRY_ROLE }}
      REGISTRY: "222.dkr.ecr.eu-west-1.amazonaws.com"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-docker-service-eu
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: Login to registry
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.DEPLOY_ROLE }}
      - name: zipx docker
        run: sbt 'service/Docker/publish'
  docker-service-us:
    name: docker service (us)
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read
    env:
      DEPLOY_ROLE: ${{ secrets.US_REGISTRY_ROLE }}
      REGISTRY: "111.dkr.ecr.us-east-1.amazonaws.com"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-docker-service-us
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: Login to registry
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.DEPLOY_ROLE }}
      - name: zipx docker
        run: sbt 'service/Docker/publish'
  deploy-prod:
    name: deploy (prod)
    runs-on: ubuntu-latest
    needs:
      - docker-service-eu
      - docker-service-us
    if: (startsWith(github.ref, 'refs/tags/v')) && (github.ref == 'refs/heads/main')
    environment: production
    permissions:
      id-token: write
      contents: read
    env:
      AWS_REGION: us-east-1
      DEPLOY_ROLE: ${{ secrets.PROD_DEPLOY_ROLE }}
      TIER: prod
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-deploy-prod
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.DEPLOY_ROLE }}
          aws-region: ${{ env.AWS_REGION }}
      - name: zipx deploy
        run: sbt 'service/promote'
  deploy-staging:
    name: deploy (staging)
    runs-on: ubuntu-latest
    needs:
      - docker-service-eu
      - docker-service-us
    if: (startsWith(github.ref, 'refs/tags/v')) && (github.ref == 'refs/heads/main')
    permissions:
      id-token: write
      contents: read
    env:
      AWS_REGION: us-west-2
      DEPLOY_ROLE: ${{ secrets.STAGING_DEPLOY_ROLE }}
      TIER: staging
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
      - name: Setup JDK 21
        uses: actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95 # v5.6.0
        with:
          distribution: temurin
          java-version: "21"
      - uses: sbt/setup-sbt@6444f4c8111de4b9059c3975def104b03cfaa5f0 # v1.5.2
        with:
          disk-cache: "false"
      - name: Cache sbt
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
            target
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-deploy-staging
          restore-keys: |
            ubuntu-latest-jdk21-sbt-1.4.2-ci-${{ github.run_id }}-
            ubuntu-latest-jdk21-sbt-1.4.2-ci-
            ubuntu-latest-jdk21-sbt-
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.DEPLOY_ROLE }}
          aws-region: ${{ env.AWS_REGION }}
      - name: zipx deploy
        run: sbt 'service/promote'
