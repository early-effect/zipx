# Generated by zipx — do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: CI
"on":
  push:
    branches:
      - main
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
  pull_request: null
jobs:
  affected:
    name: affected
    runs-on: ubuntu-latest
    outputs:
      modules: ${{ steps.compute.outputs.modules }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: "0"
      - uses: sbt/setup-sbt@v1
      - name: Compute affected modules
        id: compute
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            base="${{ github.event.pull_request.base.sha }}"
            modules=$(sbt -batch --error "zipxAffectedModules $base")
          else
            modules='["all"]'
          fi
          echo "modules=$modules" >> "$GITHUB_OUTPUT"
  test-models:
    name: test models
    runs-on: ubuntu-latest
    needs:
      - affected
      - fmt
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'models') || contains(fromJson(needs.affected.outputs.modules), 'all'))"
    strategy:
      fail-fast: false
      matrix:
        scala:
          - "2.13.16"
          - "3.8.4"
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: test
        run: sbt '++${{ matrix.scala }} models/test'
  test-coreLib:
    name: test coreLib
    runs-on: ubuntu-latest
    needs:
      - affected
      - fmt
      - test-models
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'coreLib') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.test-models.result != 'failure'"
    strategy:
      fail-fast: false
      matrix:
        scala:
          - "2.13.16"
          - "3.8.4"
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: test
        run: sbt '++${{ matrix.scala }} coreLib/test'
  test-client:
    name: test client
    runs-on: ubuntu-latest
    needs:
      - affected
      - fmt
      - test-coreLib
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'client') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.test-coreLib.result != 'failure'"
    strategy:
      fail-fast: false
      matrix:
        scala:
          - "2.13.16"
          - "3.8.4"
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: test
        run: sbt '++${{ matrix.scala }} client/test'
  test-service:
    name: test service
    runs-on: ubuntu-latest
    needs:
      - affected
      - fmt
      - test-coreLib
    if: "!cancelled() && (contains(fromJson(needs.affected.outputs.modules), 'service') || contains(fromJson(needs.affected.outputs.modules), 'all')) && needs.test-coreLib.result != 'failure'"
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: test
        run: sbt 'service/test'
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: fmt
        run: sbt 'scalafmtCheckAll'
  publish-models:
    name: publish models
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: publish
        run: sbt '+models/publish'
  publish-coreLib:
    name: publish coreLib
    runs-on: ubuntu-latest
    needs:
      - publish-models
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: publish
        run: sbt '+coreLib/publish'
  publish-client:
    name: publish client
    runs-on: ubuntu-latest
    needs:
      - publish-coreLib
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: publish
        run: sbt '+client/publish'
  docker-service-eu:
    name: docker service (eu)
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read
    env:
      DEPLOY_ROLE: ${{ secrets.EU_REGISTRY_ROLE }}
      REGISTRY: "222.dkr.ecr.eu-west-1.amazonaws.com"
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: Login to registry
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.DEPLOY_ROLE }}
      - name: docker
        run: sbt 'service/Docker/publish'
  docker-service-us:
    name: docker service (us)
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read
    env:
      DEPLOY_ROLE: ${{ secrets.US_REGISTRY_ROLE }}
      REGISTRY: "111.dkr.ecr.us-east-1.amazonaws.com"
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: Login to registry
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.DEPLOY_ROLE }}
      - name: docker
        run: sbt 'service/Docker/publish'
  deploy-service-prod:
    name: deploy service (prod)
    runs-on: ubuntu-latest
    needs:
      - docker-service-eu
      - docker-service-us
    if: (startsWith(github.ref, 'refs/tags/v')) && (github.ref == 'refs/heads/main')
    environment: production
    permissions:
      id-token: write
      contents: read
    env:
      AWS_REGION: us-east-1
      DEPLOY_ROLE: ${{ secrets.PROD_DEPLOY_ROLE }}
      TIER: prod
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.DEPLOY_ROLE }}
          aws-region: ${{ env.AWS_REGION }}
      - name: deploy
        run: sbt 'service/promote'
  deploy-service-staging:
    name: deploy service (staging)
    runs-on: ubuntu-latest
    needs:
      - docker-service-eu
      - docker-service-us
    if: (startsWith(github.ref, 'refs/tags/v')) && (github.ref == 'refs/heads/main')
    permissions:
      id-token: write
      contents: read
    env:
      AWS_REGION: us-west-2
      DEPLOY_ROLE: ${{ secrets.STAGING_DEPLOY_ROLE }}
      TIER: staging
    steps:
      - uses: actions/checkout@v4
      - uses: sbt/setup-sbt@v1
      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      - name: Cache sbt
        uses: actions/cache@v4
        with:
          path: |
            ~/.sbt
            ~/.cache/sbt
            ~/.cache/coursier
          key: ubuntu-latest-jdk21-sbt-1.4.2-ci
          restore-keys: ubuntu-latest-jdk21-sbt-
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          role-to-assume: ${{ env.DEPLOY_ROLE }}
          aws-region: ${{ env.AWS_REGION }}
      - name: deploy
        run: sbt 'service/promote'
