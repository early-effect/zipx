# Generated by zipx. Do not edit. Run 'sbt zipxWorkflowGenerate' to regenerate.
name: CI
"on":
  push:
    branches:
      - main
    tags:
      - v[0-9]+.[0-9]+.[0-9]+
  pull_request: null
concurrency:
  group: CI-${{ github.ref }}
  cancel-in-progress: ${{ !startsWith(github.ref, 'refs/tags/') }}
jobs:
  verify-gate:
    name: verify-gate
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: read
      pull-requests: read
    outputs:
      run: ${{ steps.check.outputs.run }}
    steps:
      - name: Skip Verify after merged PR push
        id: check
        run: |
          # Commits landed by merging/squashing a PR are associated with that PR via the API.
          prs=$(gh api "repos/${{ github.repository }}/commits/${{ github.sha }}/pulls" \
            --jq "[.[] | select(.merged_at != null and .base.ref == \"${{ github.ref_name }}\")] | length")
          if [ "$prs" -gt 0 ]; then
            echo "Merged PR push, skipping redundant Verify (already ran on the PR)"
            echo "run=false" >> "$GITHUB_OUTPUT"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          GH_TOKEN: ${{ github.token }}
  cache-rehydrate:
    name: cache-rehydrate
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: needs.verify-gate.result == 'success' && needs.verify-gate.outputs.run == 'false'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: cache-rehydrate
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: cache-rehydrate
        run: sbt 'compile'
  test-L0:
    name: test L0
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: test-L0
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; models/testFull'
          else
            sbt 'models/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  test-L1:
    name: test L1
    runs-on: ubuntu-latest
    needs:
      - test-L0
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: test-L1
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; coreLib/testFull'
          else
            sbt 'coreLib/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  test-L2:
    name: test L2
    runs-on: ubuntu-latest
    needs:
      - test-L1
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: test-L2
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: test
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; client/testFull; service/testFull'
          else
            sbt 'client/testFull; service/testFull'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  fmt:
    name: fmt
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: fmt
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: fmt
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; scalafmtCheckAll'
          else
            sbt 'scalafmtCheckAll'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  workflow-check:
    name: workflow-check
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: workflow-check
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: workflow-check
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxWorkflowCheck'
          else
            sbt 'zipxWorkflowCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  advisories:
    name: advisories
    runs-on: ubuntu-latest
    needs:
      - verify-gate
    if: "!cancelled() && !startsWith(github.ref, 'refs/tags/') && github.event_name != 'workflow_dispatch' && ((needs.verify-gate.result != 'success') || (needs.verify-gate.outputs.run == 'true'))"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: advisories
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: advisories
        run: |
          if [ "$ZIPX_VERIFY_CLEAN_FULL" = "true" ]; then
            sbt 'cleanFull; zipxAdvisoryCheck'
          else
            sbt 'zipxAdvisoryCheck'
          fi
        env:
          ZIPX_VERIFY_CLEAN_FULL: ${{ github.event_name == 'pull_request' && contains(github.event.pull_request.labels.*.name, 'clean') }}
  publish-L0:
    name: publish L0
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: publish-L0
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: publish
        run: sbt '+models/publish'
  publish-L1:
    name: publish L1
    runs-on: ubuntu-latest
    needs:
      - publish-L0
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: publish-L1
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: publish
        run: sbt '+coreLib/publish'
  publish-L2:
    name: publish L2
    runs-on: ubuntu-latest
    needs:
      - publish-L1
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: publish-L2
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: publish
        run: sbt '+client/publish'
  docker:
    name: docker
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      id-token: write
      contents: read
    env:
      ZIPX_EU_AWS_ACCOUNT_ID: "444455556666"
      ZIPX_EU_AWS_ECR_REGISTRY: "444455556666.dkr.ecr.eu-west-1.amazonaws.com"
      ZIPX_EU_AWS_REGION: eu-west-1
      ZIPX_EU_AWS_ROLE_TO_ASSUME: ${{ secrets.EU_REGISTRY_ROLE }}
      ZIPX_US_AWS_ACCOUNT_ID: "111122223333"
      ZIPX_US_AWS_ECR_REGISTRY: "111122223333.dkr.ecr.us-east-1.amazonaws.com"
      ZIPX_US_AWS_REGION: us-east-1
      ZIPX_US_AWS_ROLE_TO_ASSUME: ${{ secrets.US_REGISTRY_ROLE }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: docker
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: zipx AWS login (eu)
        uses: ./.github/actions/zipx-aws-login
        with:
          role-env: ZIPX_EU_AWS_ROLE_TO_ASSUME
          region-env: ZIPX_EU_AWS_REGION
          account-env: ZIPX_EU_AWS_ACCOUNT_ID
          login-ecr: "true"
          name-suffix: eu
      - name: zipx AWS login (us)
        uses: ./.github/actions/zipx-aws-login
        with:
          role-env: ZIPX_US_AWS_ROLE_TO_ASSUME
          region-env: ZIPX_US_AWS_REGION
          account-env: ZIPX_US_AWS_ACCOUNT_ID
          login-ecr: "true"
          name-suffix: us
      - name: docker
        run: sbt 'service/Docker/publish'
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    needs:
      - docker
    if: startsWith(github.ref, 'refs/tags/v')
    environment: ${{ matrix.environment }}
    permissions:
      id-token: write
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: prod
            environment: production
            AWS_REGION: us-east-1
            AWS_ROLE_TO_ASSUME: ${{ secrets.PROD_DEPLOY_ROLE }}
            TIER: prod
          - target: staging
            AWS_REGION: us-west-2
            AWS_ROLE_TO_ASSUME: ${{ secrets.STAGING_DEPLOY_ROLE }}
            TIER: staging
    env:
      AWS_REGION: ${{ matrix.AWS_REGION }}
      AWS_ROLE_TO_ASSUME: ${{ matrix.AWS_ROLE_TO_ASSUME }}
      TIER: ${{ matrix.TIER }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: "0"
          fetch-tags: "true"
      - name: zipx sbt setup
        uses: ./.github/actions/zipx-sbt-setup
        with:
          java-version: "21"
          runner-os: ubuntu-latest
          cache-key-suffix: deploy
          node-version: ""
          sbt-disk-cache: "false"
          local-cache: "true"
          cache-epoch: ""
      - name: zipx AWS login
        uses: ./.github/actions/zipx-aws-login
        with:
          role-env: AWS_ROLE_TO_ASSUME
          region-env: AWS_REGION
          account-env: AWS_ACCOUNT_ID
          login-ecr: "false"
          name-suffix: ""
      - name: deploy
        run: sbt 'service/promote'
