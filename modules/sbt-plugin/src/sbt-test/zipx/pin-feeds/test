# Catalog Pin vals: generate folds pin OSV into advisories on ci.yml plus both companions.
> zipxWorkflowGenerate
$ exists .github/workflows/ci.yml
$ exists .github/workflows/zipx-pin-check.yml
$ exists .github/workflows/zipx-pin-snapshot.yml
$ exists .github/workflows/zipx-version-updates.yml
> assertPinFeeds
> zipxWorkflowCheck
# Local update: dry-run lists only; yes rewrites the Pin constructor then materializes.
> zipxPinUpdate dry-run
$ absent applied.txt
> zipxPinUpdate yes
$ exists applied.txt
> assertPinUpdate
